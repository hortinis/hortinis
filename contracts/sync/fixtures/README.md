# Synchronization conformance fixtures

Cross-runtime synchronization examples belong in this directory. E1 introduces the first protocol
fixtures after D2 defines their canonical wire contracts.

Fixtures are language-neutral JSON data. Keep generated TypeSpec artifacts out of this directory.
