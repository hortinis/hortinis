# Generated JSON Schemas

This directory is reserved for JSON Schemas generated from TypeSpec. The first production schemas are
introduced with D2, alongside the technical synchronization contracts that use them.

Do not add hand-maintained schema copies here. Emitter compatibility probes and invalid fixtures belong
under `tooling/contracts/fixtures/`.
