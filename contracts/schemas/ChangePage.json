{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "ChangePage.json",
    "type": "object",
    "properties": {
        "changes": {
            "type": "array",
            "items": {
                "$ref": "TechnicalChange.json"
            }
        },
        "nextCursor": {
            "$ref": "SyncCursor.json"
        },
        "hasMore": {
            "type": "boolean"
        }
    },
    "required": [
        "changes",
        "nextCursor",
        "hasMore"
    ],
    "unevaluatedProperties": {
        "not": {}
    },
    "description": "A page of accepted changes and the opaque cursor following the complete page."
}